version: '3.8'

services:
  django_gunicorn:
    volumes:
      - static:/app/static
    build:
      context: .
    image: joelgxp/fox-django:0.0.1
    ports:
      - 8003:8000

  nginx:
    build: ./nginx
    image: joelgxp/fox-nginx:0.0.1
    volumes:
      - static:/static
    ports:
      - 9003:80
    depends_on:
      - django_gunicorn

volumes:
  static:
